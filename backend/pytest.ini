[pytest]
# Tell pytest where to find the app package.
# This adds the backend/ directory to sys.path so that
# "from app.models.ingest_models import ParsedFile" resolves correctly.
pythonpath = .
testpaths = tests
asyncio_mode = strict